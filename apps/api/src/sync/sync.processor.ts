import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Job } from 'bullmq';
import { PrismaService } from '../common/prisma/prisma.service';
import { ApifRsAdapter } from '../scraping/adapters/apif-rs.adapter';
import { PublicRegistryAdapter } from '../scraping/adapters/public-registry.adapter';
import { ScrapingEngine } from '../scraping/engine/scraping.engine';
@Processor('sync')
export class SyncProcessor extends WorkerHost { constructor(private prisma: PrismaService, private engine: ScrapingEngine) { super(); } async process(job: Job<{ source?: string; syncJobId: string }>) { await this.prisma.syncJob.update({ where: { id: job.data.syncJobId }, data: { status: 'RUNNING', startedAt: new Date() } }); const adapters = [new ApifRsAdapter(), new PublicRegistryAdapter()].filter(a => !job.data.source || a.name === job.data.source); let processed = 0; try { for (const adapter of adapters) { const companies = await this.engine.run(adapter); for (const item of companies) { await this.prisma.company.upsert({ where: { jib: item.jib }, update: { name: item.name, status: item.status ?? 'UNKNOWN' }, create: { name: item.name, slug: `${item.name.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-${item.jib}`, jib: item.jib, status: item.status ?? 'UNKNOWN', address: item.city ? { create: { city: item.city, street: item.address ?? 'N/A' } } : undefined } }); processed++; } } await this.prisma.syncJob.update({ where: { id: job.data.syncJobId }, data: { status: 'SUCCEEDED', finishedAt: new Date(), recordsProcessed: processed } }); } catch (error) { await this.prisma.syncJob.update({ where: { id: job.data.syncJobId }, data: { status: 'FAILED', finishedAt: new Date(), errorMessage: error instanceof Error ? error.message : 'Unknown error' } }); throw error; } } }
