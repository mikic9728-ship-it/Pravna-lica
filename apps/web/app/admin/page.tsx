import { Card } from '../../components/ui/card';
import { fetchApi } from '../../lib/api';
export default async function AdminPage() { const [jobs, status, users, subs] = await Promise.all([fetchApi<any[]>('/admin/sync-jobs').catch(()=>[]), fetchApi<any>('/admin/api-status').catch(()=>({api:'offline'})), fetchApi<any[]>('/admin/users').catch(()=>[]), fetchApi<any[]>('/admin/subscriptions').catch(()=>[])]); return <main className="mx-auto max-w-7xl px-4 py-10"><h1 className="text-4xl font-black">Admin panel</h1><div className="mt-8 grid gap-4 md:grid-cols-4"><Card><p>API status</p><b>{status.api}</b></Card><Card><p>Sync jobs</p><b>{jobs.length}</b></Card><Card><p>Korisnici</p><b>{users.length}</b></Card><Card><p>Subscriptions</p><b>{subs.length}</b></Card></div><Card className="mt-8"><div className="flex items-center justify-between"><h2 className="text-xl font-black">Sync jobovi</h2><form action="/api/admin/run-sync" method="post"><button className="rounded-xl bg-primary px-4 py-2 font-bold text-navy">Ručno pokreni sync</button></form></div><div className="mt-4 overflow-x-auto"><table className="w-full text-left"><tbody>{jobs.map(j=><tr className="border-t" key={j.id}><td className="py-3">{j.source}</td><td>{j.status}</td><td>{j.recordsProcessed}</td><td>{j.errorMessage}</td></tr>)}</tbody></table></div></Card><Card className="mt-8"><h2 className="font-black">Monitoring grešaka i scraping logova</h2><p className="mt-2 text-slate-500">Pregled audit logova, retry statusa i API zdravlja je spreman za povezivanje sa Sentry/Prometheus stackom.</p></Card></main>; }
