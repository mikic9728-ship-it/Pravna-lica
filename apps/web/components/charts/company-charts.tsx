'use client';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export function RevenueChart({ data }: { data: Array<{ year: number; revenue: number | string; profit: number | string }> }) { const rows = data.map(d => ({ ...d, revenue: Number(d.revenue), profit: Number(d.profit) })); return <ResponsiveContainer width="100%" height={280}><AreaChart data={rows}><defs><linearGradient id="rev" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#f5c542" stopOpacity={0.8}/><stop offset="95%" stopColor="#f5c542" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" opacity={0.2}/><XAxis dataKey="year"/><YAxis/><Tooltip/><Area type="monotone" dataKey="revenue" stroke="#f5c542" fill="url(#rev)"/></AreaChart></ResponsiveContainer>; }
export function EmployeeChart({ data }: { data: Array<{ year: number; count: number }> }) { return <ResponsiveContainer width="100%" height={260}><BarChart data={data}><CartesianGrid strokeDasharray="3 3" opacity={0.2}/><XAxis dataKey="year"/><YAxis/><Tooltip/><Bar dataKey="count" fill="#38bdf8" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer>; }
